<!-- body of the page -->
{{!-- <div id="currentUser" class="d-flex">
	<p id="username">{{user.username }}- </p>
	<p id="winRecord">
		{{!-- {{ (1/4) }} --}}
		{{!-- <span>Wins:</span>
		{{user.wins}}
		<Span>Ratio:</Span>
		{{winratio}}
		is logged in: {{isLoggedIn}} --}}
		{{!--
	</p>
</div> --}}
<div id="mainpage">
	<div class="d-flex sidebar">
		<div id="leaderboardSidebar" class="align-self-start">
			<!-- holds the online/playable users? -->
			<table id="leaderboard">
				<tr>
					<th>Leaderboard:</th>
				</tr>
				{{#each users as |x| }}
				{{!-- accesses partial to populate table --}}
				{{> leaderboard_row}}
				{{/each}}
			</table>
		</div>
	</div>
	
	<div id="gameField">
		<button id="playBtn">Play</button>
		<section id="gameGUI" class="align-self-end">
			<div class="score">
				<div class="playerScore">
					<h2>Player</h2>
					<p class="p-count count">0</p>

				</div>
				<div class="computerScore">
					<h2>Computer</h2>
					<p class="c-count count">0</p>

				</div>
			</div>

			<div class="move">Choose your move</div>

			<!--Number of moves left before game ends -->
			<div class="movesleft">Moves Left: 10 </div>

			<!--Options available to player to play game -->
			<div class="options">
				<button id="rockBtn">Rock</button>
				<button id="paperBtn">Paper</button>
				<button id="scissorBtn">Scissors</button>
			</div>

			<!--Final result of game -->
			<div id="result"></div>

			<!--Reload the game -->
			<button class="reload">Quit</button>
		</section>
	</div>
	{{! <button id="playBtn">Play</button> }}
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="assets/js/game.js"></script>
{{!
<script src="/assets/js/index.js"></script>
<script src="/assets/js/home.js"></script> }}